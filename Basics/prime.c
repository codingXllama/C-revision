/*

    Prime number: a number is prime iff it's divisior is 1 and it's self


*/

#include <stdio.h>
#include <math.h>

int isPrime(int userNumber)

{
    int counter;
    for (counter = 2; counter <= sqrt(userNumber); counter++) 
}

int main(int argc, char const *argv[])
{
    /* code */

    return 0;
}
